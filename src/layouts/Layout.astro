---
interface Props {
  title: string;
  summary: string;
  image: string;
  url: string;
  currentLang: string;
}

const { title, summary, image, url, currentLang } = Astro.props;
---

<!doctype html>
<html lang={currentLang}>
  <head>
    <meta charset="UTF-8" />
    <title>{title.trim()}</title>
    <meta name="description" content={summary} />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="preload" as="image" href={image} />

    <meta property="og:url" content={url} />
    <meta property="og:type" content="website" />
    <meta property="og:title" content={title.trim()} />
    <meta property="og:description" content={summary} />
    <meta property="og:image" content="" />

    <meta name="twitter:card" content="summary_large_image" />
    <meta property="twitter:domain" content={url.split("//")[1] ?? ""} />
    <meta property="twitter:url" content={url} />
    <meta name="twitter:title" content={title.trim()} />
    <meta name="twitter:description" content={summary} />
    <meta name="twitter:image" content="" />

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Huninn&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <slot />
  </body>
</html>

<style is:global>
  html {
    font-family:
      Menlo,
      Monaco,
      Lucida Console,
      "Courier New",
      Courier,
      monospace;
    background: #fff;
    letter-spacing: -0.025rem;
    overflow-x: hidden;
  }

  html:lang(zh_tw) {
    font-family:
      Huninn,
      Menlo,
      Monaco,
      Lucida Console,
      "Courier New",
      Courier,
      monospace;
  }

  body,
  figure {
    margin: 0;
    padding: 0;
  }

  body {
    max-width: 100vw;
    overflow-x: hidden;
  }

  a {
    text-decoration: none;
  }

  ul {
    list-style: none;
    margin: 0;
    padding: 0;
  }

  *,
  *::before,
  *::after {
    box-sizing: border-box;
  }

  h1,
  h2,
  h3,
  h4 {
    margin: 0;
    font-family:
      system-ui,
      -apple-system,
      BlinkMacSystemFont,
      "Segoe UI",
      Roboto,
      Oxygen,
      Ubuntu,
      Cantarell,
      "Open Sans",
      "Helvetica Neue",
      sans-serif;
  }

  html:lang(zh_tw) :is(h1, h2, h3, h4) {
    font-family:
      Huninn,
      system-ui,
      -apple-system,
      BlinkMacSystemFont,
      "Segoe UI",
      Roboto,
      Oxygen,
      Ubuntu,
      Cantarell,
      "Open Sans",
      "Helvetica Neue",
      sans-serif;
  }

  p {
    color: #666;
    font-size: 0.9rem;
    line-height: 1.35;
    margin: 0;
    text-wrap: pretty;
  }

  .print {
    display: none !important;
  }

  @media print {
    .no-print {
      display: none !important;
    }

    .print {
      display: block !important;
    }

    astro-dev-toolbar {
      display: none !important;
    }

    article {
      break-inside: avoid;
    }

    @page {
      size: A4;
      margin: 12mm 16mm;
    }

    :root {
      font-size: 14px;
    }
  }
  :root {
    --gray: rgb(108, 117, 125);
    --yellow: rgb(255, 193, 7);
    --teal: rgb(23, 162, 184);
    --green: rgb(40, 167, 69);

    /* Fundamental */
    --skill-fundamental-bg: var(--gray);
    --skill-fundamental-color: #ffffff;
    --skill-fundamental-border: var(--gray);
    --skill-fundamental-hover-shadow: rgba(108, 117, 125, 0.25);
    --skill-fundamental-hover-border: var(--gray);
    /* Intermediate */
    --skill-intermediate-bg: var(--teal);
    --skill-intermediate-color: #ffffff;
    --skill-intermediate-border: var(--teal);
    --skill-intermediate-hover-shadow: rgba(23, 162, 184, 0.25);
    --skill-intermediate-hover-border: var(--teal);
    /* Proficient */
    --skill-proficient-bg: var(--green);
    --skill-proficient-color: #ffffff;
    --skill-proficient-border: var(--green);
    --skill-proficient-hover-shadow: rgba(40, 167, 69, 0.25);
    --skill-proficient-hover-border: var(--green);
    /* Expert */
    --skill-expert-bg: var(--yellow);
    --skill-expert-color: #212529;
    --skill-expert-border: var(--yellow);
    --skill-expert-hover-shadow: rgba(255, 193, 7, 0.25);
    --skill-expert-hover-border: var(--yellow);
  }
</style>
